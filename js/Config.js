// this is a configuration class
// it has several static values that
// should be available everywhere
function Config(){
}

// the server location
// every path shoudl be built this way:
// Config.server + <relative_path>

Config.server = "http://localhost/mads/";

